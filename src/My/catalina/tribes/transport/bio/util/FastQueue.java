package My.catalina.tribes.transport.bio.util;

/**
 * A fast queue that remover thread lock the adder thread. <br/>Limit the queue
 * length when you have strange producer thread problemes.
 */

public class FastQueue {

	private static My.juli.logging.Log log = My.juli.logging.LogFactory.getLog(FastQueue.class);

	
}
